# How to run
### 1. Create virtual environment
```shell
python -m venv venv
```
### 2. Enter venv && Install packages:
```shell
pip install -r requirements.txt
```
### 3. Run the server
```shell
python app.py
```